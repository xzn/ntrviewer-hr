on:
  workflow_dispatch:

jobs:
  build-rashader-intel:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v5
        with:
          path: ntrviewer-hr
      - uses: actions/checkout@v5
        with:
          repository: xzn/librashader
          path: librashader
      - name: Compile NTRViewer-HR
        run: |
          ntrviewer-hr/macos-scripts/compile-rashader.sh
          mkdir -p rashader-intel/include
          ln -s ../../librashader/include/librashader.h rashader-intel/include/librashader.h
          mkdir rashader-intel/lib
          ln -s ../../librashader/target/optimized/liblibrashader_capi.dylib rashader-intel/lib/librashader.dylib
      - uses: actions/upload-artifact@v4
        with:
          name: rashader-intel
          path: rashader-intel
