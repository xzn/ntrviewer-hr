on:
  workflow_dispatch:

jobs:
  build-linux-rashader-arm:
    runs-on: ubuntu-22.04-arm
    steps:
      - uses: actions/checkout@v5
        with:
          path: ntrviewer-hr
      - uses: actions/checkout@v5
        with:
          repository: SnowflakePowered/librashader
          path: librashader
          ref: librashader-v0.10.0
      - name: Compile librashader
        run: |
          apt update
          apt install -y --fix-missing curl gcc g++ clang
          curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain none
          . "$HOME/.cargo/env"
          ntrviewer-hr/linux-scripts/compile-rashader.sh
          mkdir -p linux-rashader-arm/include
          ln -s ../../librashader/include/librashader.h linux-rashader-arm/include/librashader.h
          mkdir linux-rashader-arm/lib
          ln -s ../../librashader/target/optimized/liblibrashader_capi.a linux-rashader-arm/lib/librashader.a
      - uses: actions/upload-artifact@v4
        with:
          name: linux-rashader-arm
          path: linux-rashader-arm
